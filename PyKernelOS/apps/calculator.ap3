# Calculator.ap3

def calculator():
    while True:
        expression = input("Enter a mathematical expression (or 'exit' to go back): ")
        if expression.lower() == 'exit':
            break
        try:
            result = eval(expression)
            print("Result:", result)
        except Exception as e:
            print("Error:", e)

calculator()
